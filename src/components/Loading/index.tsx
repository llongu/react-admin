import React, { useLayoutEffect, useEffect } from "react"

export default () => {
  // useEffect(() => {})
  // useLayoutEffect((): any => {
  //   // return false
  // })
  return <div>Loading...</div>
}
